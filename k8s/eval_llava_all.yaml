# LLaVA-Next full eval: ALL conditions (frozen + lora_only + reinspection via
# eval_compare=true) on the default 13-benchmark suite, 1024-token decode.
# Checkpoint: models/llava_next/grounding/stage2/epoch_4 — module + LoRA from
# the SAME dir (loader contract: never mix stages). Tiling flag is a no-op for
# LLaVA (AnyRes intrinsic). Launch with `kubectl create -f` (generateName).
#
# bs=16, not 64: the first LLaVA smoke (bs1-vs-bs64, after the AnyRes collate
# fix) showed 5/128 greedy flips — both directions, consistent with FP tie
# noise from large left-pad spans, but above the InternVL3 bs=16 precedent
# (0-1/64). bs=16 trims pad spans and matches the validated setting; H100
# instead of H200 (bs=16 fits 80GB; H200 queue was blocking admission).
apiVersion: batch/v1
kind: Job
metadata:
  generateName: reinspection-eval-llava-all-
  namespace: eidf098ns
  labels:
    kueue.x-k8s.io/queue-name: eidf098ns-user-queue
spec:
  ttlSecondsAfterFinished: 36000
  parallelism: 1
  completions: 1
  backoffLimit: 0
  template:
    metadata:
      name: reinspection-eval-llava-all
    spec:
      containers:
      - name: reinspection-eval-llava-all
        image: stogian/l_reinspection:latest
        workingDir: /data/users/stogian/learnable-reinspection
        command:
          - bash
          - -c
          - |
            set -euo pipefail
            mkdir -p logs outputs/llava_next/grounding_s2e4_1024
            export OMP_NUM_THREADS=4
            export HYDRA_FULL_ERROR=1
            export HF_HOME=/data/Huggingface
            export PYTHONPATH="${PWD}:${PYTHONPATH:-}"
            export WANDB_MODE=offline
            export PYTHONDONTWRITEBYTECODE=1

            find src -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true

            CKPT=models/llava_next/grounding/stage2/epoch_4

            bash src/scripts/run_eval.sh \
              backend=llava_next \
              checkpoint_dir=${CKPT} \
              lora_checkpoint_dir=${CKPT} \
              output_file=outputs/llava_next/grounding_s2e4_1024/eval_stage2_epoch4_compare.json \
              eval_condition=reinspection \
              eval_compare=true \
              eval_batch_size=16 \
              eval_max_new_tokens=1024 \
              pbar_enabled=true \
              2>&1 | tee logs/eval_llava_all_stage2_epoch4_1024.log
        imagePullPolicy: Always
        env:
          - { name: HF_TOKEN,      valueFrom: { secretKeyRef: { name: reinspection-secrets, key: HF_TOKEN } } }
          - { name: WANDB_API_KEY, valueFrom: { secretKeyRef: { name: reinspection-secrets, key: WANDB_API_KEY } } }
          - { name: PBAR_API_KEY,  valueFrom: { secretKeyRef: { name: reinspection-secrets, key: PBAR_API_KEY, optional: true } } }
        resources:
          requests: { cpu: 32, memory: "96Gi", nvidia.com/gpu: 1 }
          limits:   { cpu: 32, memory: "96Gi", nvidia.com/gpu: 1 }
        volumeMounts:
          - { mountPath: /dev/shm, name: dshm }
          - { mountPath: /data,    name: nfs }
      restartPolicy: Never
      securityContext: { runAsUser: 30545, runAsGroup: 4451, runAsNonRoot: true }
      volumes:
      - name: dshm
        emptyDir: { medium: Memory, sizeLimit: "32Gi" }
      - name: nfs
        nfs: { server: 10.24.6.66, path: / }
      nodeSelector:
        nvidia.com/gpu.product: NVIDIA-H100-80GB-HBM3
