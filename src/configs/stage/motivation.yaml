# @package _global_
# Motivation experiment: spatial minimal-pair flip consistency + attention divergence
#
# Usage:
#   python -m src.motivation stage=motivation backend=internvl3
#   python -m src.motivation stage=motivation backend=internvl3 checkpoint_dir=models/internvl3/stage2/epoch_4
#   python -m src.motivation stage=motivation backend=internvl3 max_samples=200

stage: 2
checkpoint_dir: null
lora_checkpoint_dir: null
output_file: outputs/motivation/motivation_results.json
eval_condition: frozen
max_samples: -1
