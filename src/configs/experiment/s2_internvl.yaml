# @package _global_
# Stage 2 — spatial VQA. InternVL3. Default mix.
# Edit `stage1_checkpoint` to match the Stage-1 epoch you want to use.
defaults:
  - override /backend: internvl3
  - override /stage: stage2
  - override /stage2_datasets: default

stage1_checkpoint: models/internvl3/stage1/epoch_5/reinspection_module.pt
wandb_run_name: s2_internvl
experiment_name: s2_internvl
