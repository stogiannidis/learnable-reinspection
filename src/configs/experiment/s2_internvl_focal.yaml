# @package _global_
# Stage 2 ablation — InternVL3 from focal Stage-1 checkpoint.
# Edit `stage1_checkpoint` to match the epoch you want to use.
defaults:
  - override /backend: internvl3
  - override /stage: stage2
  - override /stage2_datasets: default

stage1_checkpoint: models/internvl3/focal/stage1/epoch_5/reinspection_module.pt
wandb_run_name: internvl-stage2-focal
experiment_name: focal
